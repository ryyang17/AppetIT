# .gitlab-ci.yml
stages:
  - build
  - test
  - package

before_script:
  - java -version
  - ./gradlew --version

# Build stage
build:
  stage: build
  script:
    - ./gradlew clean assemble --no-deamon
  artifacts:
    paths:
      - build/libs/
    expire_in: 1 week

# Test stage
test:
  stage: test
  script:
    - ./gradlew test --no-deamon
  artifacts:
    reports:
      junit:
        - build/test-results/test/*.xml
  allow_failure: true

# Package stage
package:
  stage: package
  script:
    - ./gradlew bootJar --no-deamon
  artifacts:
    paths:
      - build/libs/*.jar
    expire_in: 1 week
